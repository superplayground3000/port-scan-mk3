---
phase: 03-support-multiple-pressure-sources-and-provide-cross-platform-run-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/config/config.go
  - pkg/config/config_test.go
  - pkg/config/pressure_sources.go
  - pkg/config/pressure_sources_test.go
  - pkg/scanapp/pressure.go
  - pkg/scanapp/pressure_factory.go
  - pkg/scanapp/pressure_factory_test.go
  - pkg/scanapp/pressure_multi_test.go
  - cmd/port-scan/command_handlers.go
autonomous: true
requirements:
  - PRESSURE-03
must_haves:
  truths:
    - "使用者可在單次 scan 指令中提供多個 pressure source。"
    - "多來源壓力值以明確 policy 合成單一 pause/resume 判斷值。"
    - "舊有單來源旗標流程仍可執行且語意不變。"
  artifacts:
    - path: "pkg/config/pressure_sources.go"
      provides: "多來源旗標字串解析與驗證"
    - path: "pkg/scanapp/pressure_factory.go"
      provides: "由 config 建立單一或多來源 PressureFetcher"
    - path: "cmd/port-scan/command_handlers.go"
      provides: "CLI 只做組裝，委派 pkg 建立 fetcher"
  key_links:
    - from: "pkg/config/config.go"
      to: "pkg/scanapp/pressure_factory.go"
      via: "Config.PressureSources + Config.PressurePolicy"
      pattern: "PressureSources|PressurePolicy"
    - from: "cmd/port-scan/command_handlers.go"
      to: "pkg/scanapp/pressure_factory.go"
      via: "scanapp.BuildPressureFetcherFromConfig"
      pattern: "BuildPressureFetcherFromConfig"
---

<objective>
完成 PRESSURE-03：支援單次掃描配置多個 pressure source，並以可選 policy 產生一致的 pause/resume 決策。

Purpose: 將多來源行為封裝在 `pkg/`，維持 `cmd/port-scan` 僅做 CLI 組裝，符合 SOLID 與專案 guardrails。  
Output: 新增多來源 config contract、聚合 fetcher/factory 與對應測試，保留舊旗標完全相容。
</objective>

<execution_context>
@/Users/xuxiping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xuxiping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-support-multiple-pressure-sources-and-provide-cross-platform-run-scripts/03-CONTEXT.md
@.planning/phases/03-support-multiple-pressure-sources-and-provide-cross-platform-run-scripts/03-RESEARCH.md
@AGENTS.md
@pkg/config/config.go
@pkg/scanapp/pressure.go
@pkg/scanapp/pressure_monitor.go
@cmd/port-scan/command_handlers.go

<interfaces>
From pkg/scanapp/pressure.go:
```go
type PressureFetcher interface {
	Fetch(ctx context.Context) (float64, error)
}
```

From pkg/scanapp/scan.go:
```go
type RunOptions struct {
	PressureFetcher PressureFetcher
}
```

From pkg/config/config.go:
```go
type Config struct {
	PressureAPI          string
	PressureUseAuth      bool
	PressureAuthURL      string
	PressureDataURL      string
	PressureClientID     string
	PressureClientSecret string
	PressureInterval     time.Duration
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定義多來源 CLI/config 合約與回溯相容規則</name>
  <read_first>
pkg/config/config.go
pkg/config/config_test.go
.planning/phases/03-support-multiple-pressure-sources-and-provide-cross-platform-run-scripts/03-CONTEXT.md
.planning/phases/03-support-multiple-pressure-sources-and-provide-cross-platform-run-scripts/03-RESEARCH.md
AGENTS.md
  </read_first>
  <files>pkg/config/config.go, pkg/config/config_test.go, pkg/config/pressure_sources.go, pkg/config/pressure_sources_test.go</files>
  <behavior>
    - Test 1: 重複 `-pressure-source` 可解析為 2 筆來源，且保留輸入順序。
    - Test 2: `-pressure-policy` 僅接受 `max|min|avg`，預設值為 `max`。
    - Test 3: 未提供 `-pressure-source` 時，自動從舊旗標產生單一來源（simple 或 auth）。
    - Test 4: `-pressure-source` 中 auth 來源缺少必要鍵值時回傳明確錯誤。
  </behavior>
  <action>
實作 D-01（多來源同次執行）與 D-04（cmd 僅組裝）：
- 在 `Config` 新增 `PressureSources []PressureSourceSpec` 與 `PressurePolicy string`。
- 新增 repeatable flag：`-pressure-source`（可多次提供），新增單值旗標：`-pressure-policy`（default=`max`）。
- 在 `pkg/config/pressure_sources.go` 實作 parser，來源格式固定為 `kind=<simple|auth>,key=value,...`：
  - simple 必填：`kind=simple,pressure-api=<url>`
  - auth 必填：`kind=auth,auth-url=<url>,data-url=<url>,client-id=<id>,client-secret=<secret>`
- 在 `Parse` 內加入 backward compatibility：若 `PressureSources` 為空，沿用既有 `-pressure-use-auth`/`-pressure-api` 旗標組合自動組出 1 筆來源，行為需與現況一致。
- 保留現有舊旗標，不可刪除、不可改名、不可改語意。
  </action>
  <verify>
    <automated>go test ./pkg/config -run 'Pressure' -count=1</automated>
  </verify>
  <acceptance_criteria>
- `rg -n 'PressureSources\\s+\\[\\]PressureSourceSpec|PressurePolicy\\s+string' pkg/config/config.go` 有命中。
- `rg -n 'fs\\.Func\\(\"pressure-source\"|StringVar\\(&cfg\\.PressurePolicy, \"pressure-policy\"' pkg/config/config.go` 有命中。
- `go test ./pkg/config -run 'Pressure' -count=1` exit code = 0。
  </acceptance_criteria>
  <done>Config 可同時承載多來源與 policy，且舊單來源旗標行為維持可用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 scanapp 新增多來源聚合與 policy 實作</name>
  <read_first>
pkg/scanapp/pressure.go
pkg/scanapp/pressure_monitor.go
pkg/scanapp/scan.go
pkg/scanapp/pressure_test.go
AGENTS.md
  </read_first>
  <files>pkg/scanapp/pressure.go, pkg/scanapp/pressure_multi_test.go</files>
  <behavior>
    - Test 1: policy=`max` 時回傳來源壓力最大值。
    - Test 2: policy=`min` 時回傳來源壓力最小值。
    - Test 3: policy=`avg` 時回傳平均值（套用既有一位小數 round）。
    - Test 4: 單一來源失敗但仍有成功樣本時，不回傳 fatal error。
    - Test 5: 所有來源都失敗時，回傳 error 供 monitor 延續現有 3 次失敗升級邏輯。
  </behavior>
  <action>
實作 D-01 的 runtime 聚合策略：
- 在 `pkg/scanapp/pressure.go` 新增 `MultiPressureFetcher`（或等價封裝）與 policy 解析，支援 `max|min|avg`。
- `Fetch(ctx)` 必須對所有來源取樣，採「有成功樣本就依 policy 合成；全部失敗才 error」語義。
- 聚合結果沿用既有 `normalizePressure` 一位小數規則。
- 不變更 `PressureFetcher` 介面簽章，避免波及 monitor state machine。
  </action>
  <verify>
    <automated>go test ./pkg/scanapp -run 'Pressure|Multi' -count=1</automated>
  </verify>
  <acceptance_criteria>
- `rg -n 'type MultiPressureFetcher|func .*Combine|policy' pkg/scanapp/pressure.go` 有命中。
- `rg -n 'max|min|avg' pkg/scanapp/pressure.go` 有命中。
- `go test ./pkg/scanapp -run 'Pressure|Multi' -count=1` exit code = 0。
  </acceptance_criteria>
  <done>scanapp 具備多來源聚合能力，且 failure 語義可與現有 monitor 故障升級機制兼容。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 在 CLI 組裝層接線多來源 fetcher，維持 cmd 薄層</name>
  <read_first>
cmd/port-scan/command_handlers.go
pkg/config/config.go
pkg/scanapp/pressure.go
pkg/scanapp/pressure_monitor.go
AGENTS.md
  </read_first>
  <files>pkg/scanapp/pressure_factory.go, pkg/scanapp/pressure_factory_test.go, cmd/port-scan/command_handlers.go</files>
  <behavior>
    - Test 1: 提供 2 筆 `PressureSources` 時，factory 回傳可用的 multi fetcher。
    - Test 2: 僅提供舊旗標衍生來源時，factory 回傳與原本一致的 simple/auth fetcher。
    - Test 3: `runScan` 不含業務解析邏輯，只呼叫 `pkg/scanapp` factory 取得 fetcher。
  </behavior>
  <action>
實作 D-04（CLI 僅組裝）：
- 在 `pkg/scanapp/pressure_factory.go` 新增 `BuildPressureFetcherFromConfig(cfg config.Config, client *http.Client) (PressureFetcher, error)`，負責：
  - 將 `cfg.PressureSources` 逐筆轉為 simple/auth fetcher。
  - 來源數 >1 時建立 `MultiPressureFetcher` 並套用 `cfg.PressurePolicy`。
  - 來源數 =1 時直接回傳單一 fetcher。
- `cmd/port-scan/command_handlers.go` 的 `runScan` 僅呼叫上述 factory，禁止在 cmd 寫來源解析/policy 邏輯。
- 更新 `usage()` 旗標字串，加入 `-pressure-source` 與 `-pressure-policy`，並保留舊旗標列出。
  </action>
  <verify>
    <automated>go test ./pkg/scanapp ./cmd/port-scan -run 'Pressure|RunMain' -count=1</automated>
  </verify>
  <acceptance_criteria>
- `rg -n 'BuildPressureFetcherFromConfig' cmd/port-scan/command_handlers.go` 有命中。
- `rg -n 'pressure-source|pressure-policy' cmd/port-scan/command_handlers.go` 有命中。
- `go test ./pkg/scanapp ./cmd/port-scan -run 'Pressure|RunMain' -count=1` exit code = 0。
  </acceptance_criteria>
  <done>cmd 層僅負責 config 與 I/O 組裝；多來源與 policy 邏輯全在 pkg 層完成。</done>
</task>

</tasks>

<verification>
- `go test ./pkg/config -run 'Pressure' -count=1`
- `go test ./pkg/scanapp -run 'Pressure|Multi' -count=1`
- `go test ./pkg/scanapp ./cmd/port-scan -run 'Pressure|RunMain' -count=1`
</verification>

<success_criteria>
- 可在同次指令提供至少兩個 `-pressure-source` 並成功解析。
- `-pressure-policy` 可控制多來源合成（`max|min|avg`），預設 `max`。
- 舊旗標流程在未提供 `-pressure-source` 時仍可運作。
</success_criteria>

<output>
After completion, create `.planning/phases/03-support-multiple-pressure-sources-and-provide-cross-platform-run-scripts/03-01-SUMMARY.md`
</output>
